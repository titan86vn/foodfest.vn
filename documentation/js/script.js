 
(function($) {
        "use strict";
window.onload = function() {

   window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css2"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

    window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css3"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

    window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css4"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

    window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css5"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

    window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css6"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

   window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css7"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 

   window.editor_markdown = CodeMirror.fromTextArea(document.getElementById("code-css8"), {
    mode: "css",
    lineNumbers: true,
    lineWrapping: true
  }); 


  
    window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html3"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
    window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html4"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
   
  
       window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html5"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
     window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html6"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });

      window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html7"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });

    window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html8"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
   
   window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html9"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
   window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html10"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
  

  window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html11"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
  
  
 
 
    window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html13"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
    window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html14"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
 
  
      window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html16"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
 
  
      window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html18"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });   

  window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html18_2"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
   window.editor_html = CodeMirror.fromTextArea( document.getElementById("code-html19"), {
    mode: "text/html",
    lineNumbers: true,
    lineWrapping: true,
    extraKeys: {"Ctrl-Q": function(cm){ cm.foldCode(cm.getCursor()); }},
    foldGutter: true,
    gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"]
  });
  
 
}; 
})(jQuery);